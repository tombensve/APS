package se.natusoft.osgi.aps.api.reactive;

/**
 * For those that prefer to have an "Async" part in the name ...
 *
 * @param <T> The type held.
 */
public interface APSAsyncValue<T> extends APSValue<T> {}
